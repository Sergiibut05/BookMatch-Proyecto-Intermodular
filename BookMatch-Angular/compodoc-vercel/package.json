{
  "name": "bookmatch-compodoc-vercel",
  "private": true,
  "version": "1.0.0",
  "description": "Vercel build wrapper for Compodoc static site",
  "scripts": {
    "build": "compodoc -p ../tsconfig.doc.json --output public --disableCoverage --disableGraph"
  },
  "devDependencies": {
    "@compodoc/compodoc": "^1.2.1"
  }
}
